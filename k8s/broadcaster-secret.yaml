apiVersion: v1
kind: Secret
metadata:
  name: broadcaster-secret
  labels:
    app: broadcaster
stringData:
  # Replace this with your real webhook URL.
  #  - Discord: create a webhook in a channel's Integrations settings and
  #    paste the URL here (payload sent is {"content": "..."}).
  #  - Slack: an Incoming Webhook URL (payload {"text": "..."}).
  #  - Generic: any URL that accepts {"user": "bot", "message": "..."}.
  # The TARGET env var in deployment-broadcaster.yaml selects the format.
  WEBHOOK_URL: "https://discord.com/api/webhooks/1525437691406061709/60x9mBea2Y5_jAvIqvWiCb47Gm_IHufa4gv04Ng91t8EQmAGYKlupEnUnSFProuFFhYP"
